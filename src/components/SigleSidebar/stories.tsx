import { Story, Meta } from '@storybook/react/types-6-0'
import SigleSidebar from '.'

export default {
  title: 'SigleSidebar',
  component: SigleSidebar
} as Meta

export const Default: Story = () => <SigleSidebar />
